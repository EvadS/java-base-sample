# Codehaus JSON

Codehaus JSON refers primarily to the legacy (1.x) versions of the Jackson JSON processor, often found under
the org.codehaus.jackson package, which was the standard JSON library for Java before
moving to FasterXML (com.fasterxml.jackson). It includes high-performance data binding (ObjectMapper)
and streaming APIs used to map Java POJOs to/from JSON.